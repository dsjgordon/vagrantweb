[core]
	editor = <%= scope.function_options_lookup(['editor', 'vi']) %>
[color]
	branch = auto
	diff = auto
	interactive = auto
	status = auto
[user]
	name = <%= scope.function_options_lookup(['name', 'developer']) %>
	email = <%= scope.function_options_lookup(['email', 'dev@example.dev']) %>